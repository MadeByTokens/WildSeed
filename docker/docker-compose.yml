services:
  # Full WildSeed with all features (GDAL + Blender + Gazebo)
  wildseed:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: wildseed:latest
    volumes:
      - ../models:/workspace/models
      - ../worlds:/workspace/worlds
      - ../Blender-Assets:/workspace/Blender-Assets:ro
    environment:
      - WILDSEED_BASE_PATH=/workspace
      - WILDSEED_BLENDER_PATH=/opt/blender/blender
      - GZ_SIM_RESOURCE_PATH=/workspace/models
    working_dir: /workspace

  # Development environment
  wildseed-dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dev
    image: wildseed:dev
    volumes:
      - ..:/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    environment:
      - WILDSEED_BASE_PATH=/workspace
      - WILDSEED_BLENDER_PATH=/opt/blender/blender
      - GZ_SIM_RESOURCE_PATH=/workspace/models
      - DISPLAY=${DISPLAY}
    working_dir: /workspace
    stdin_open: true
    tty: true
    network_mode: host

  # Terrain generation service
  terrain:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: wildseed:latest
    volumes:
      - ../models/ground:/workspace/models/ground
    environment:
      - WILDSEED_BASE_PATH=/workspace
    working_dir: /workspace
    entrypoint: ["wildseed", "terrain"]

  # Asset conversion service (with Blender)
  convert:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: wildseed:latest
    volumes:
      - ../Blender-Assets:/workspace/Blender-Assets:ro
      - ../models:/workspace/models
    environment:
      - WILDSEED_BASE_PATH=/workspace
      - WILDSEED_BLENDER_PATH=/opt/blender/blender
    working_dir: /workspace
    entrypoint: ["wildseed", "convert"]

  # Forest generation service
  generate:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: wildseed:latest
    volumes:
      - ../models:/workspace/models:ro
      - ../worlds:/workspace/worlds
    environment:
      - WILDSEED_BASE_PATH=/workspace
    working_dir: /workspace
    entrypoint: ["wildseed", "generate"]

  # Launch Gazebo with GUI (requires X11)
  launch:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: wildseed:latest
    volumes:
      - ../models:/workspace/models:ro
      - ../worlds:/workspace/worlds:ro
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    environment:
      - WILDSEED_BASE_PATH=/workspace
      - GZ_SIM_RESOURCE_PATH=/workspace/models
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
    working_dir: /workspace
    network_mode: host
    entrypoint: ["wildseed", "launch"]
